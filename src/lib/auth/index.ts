/**
 * Central export for authentication utilities
 */

// Client-side utilities
export * from "./session";

// Note: Server-side utilities should be imported directly
// from '@/lib/auth/server' to ensure they only run on server
